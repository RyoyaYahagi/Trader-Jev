# Gate 3 — Research Pipeline E2E & Fair Comparison

- Date: 2026-09-21
- Commit: `66c2847`
- Branch: `codex/issue-10-comparison`
- Dataset / fixture: `tests/conftest.py::quote`, `tests/test_comparison.py::event_stream`, existing Jev/ML integration fixtures
- Config hash: `86ad7c0711d63693f843b0e09d483b3d62aed1a74bc38f0332bd417280b3e4ac`
- Result: **PASS**

## Verification

```text
UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q
55 passed
UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests
All checks passed!
UV_CACHE_DIR=/tmp/uv-cache uv run pyright
0 errors, 0 warnings, 0 informations
```

The Phase 8 tests and existing integration tests cover:

- one common market-event stream with deterministic availability ordering
- Rule, Jev-only, ML-only, and Jev+ML comparison slots
- one immutable signal/`TradeIntent` stream forked into multiple Paper portfolios
- capital/position policy branches and Paper execution assumptions
- reproducible run/config hashing and data-range/universe filtering
- Jev timeout/error fail-closed behavior and ML-unavailable Jev-only behavior
- News integration and ML A/B/C paths from the preceding phases

Known limitations are intentionally Paper-only: no external Jev service,
broker API, or live/shadow connectivity is used. More detailed calibration and
confidence-bin reports remain downstream analysis work.
