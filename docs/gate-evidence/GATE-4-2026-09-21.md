# Gate 4 — Risk & Long-running Paper Readiness

- Date: 2026-09-21
- Commit: `a12bbd1`
- Branch: `codex/issue-11-risk-engine`
- Dataset / fixture: `tests/conftest.py::quote`, `tests/test_risk.py`, `tests/test_risk_engine.py`, and `tests/test_paper_execution.py`
- Config hash: `edb6e4237a5871232ed10a1a6bc6d8b9327f0f8193e8683dc3bfae1568d767ce`
- Result: **PASS**

## Verification

```text
UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q
59 passed
UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests
All checks passed!
UV_CACHE_DIR=/tmp/uv-cache uv run pyright
0 errors, 0 warnings, 0 informations
```

The Phase 9 tests cover:

- every pipeline-generated `OrderIntent` crossing the Risk Engine before the Paper broker
- Conservative / Balanced / Aggressive profile switching
- max position, open-order, order-notional, position-notional, cash, lot-size, spread, daily-loss, and drawdown limits
- stale market data, stale news, malformed model metadata, and future metadata rejection
- duplicate-order prevention and decision cooldown
- process restart state recovery through serialized runtime state
- consecutive-error circuit breaker, global kill switch, symbol/control fail-closed behavior, and no-broker-on-rejection
- 120 sequential unique decisions without duplicate approvals or state drift in the endurance test
- Replay → Decision → RiskEngine → PaperBroker → Fill → Portfolio E2E from Gate 2

Known limitations are intentionally Paper-only: no broker API, account authentication,
live order path, or real-time process-duration soak test is included. A production
long-running deployment and future live milestone require separate operational and
broker-specific gates.

## Post-Gate-3 dependency recheck

After the Phase 8 comparison branch was merged, the same Gate 4 commands were
rerun on merge commit `7d1ebd2`; the full suite increased to **63 passed** and
Ruff/Pyright remained clean. The Risk Engine implementation and its evidence
remain unchanged by the comparison merge.
